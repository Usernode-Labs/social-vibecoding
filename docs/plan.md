# Interface reform plan

This is the durable, manager-altitude view of the interface reform. Stable
anchors are append-only identifiers for `Plan-Step:` commit trailers. Detailed
batch evidence remains in repository research records, gate artifacts, and the
Buzz channel canvas.

Status values: **complete**, **in progress**, **deferred**.

<a id="prerequisites"></a>
## 0. Prerequisites — complete

The design baseline, five interface laws, and seven owner decisions are
ratified and recorded in the repository.

<a id="design-authority-foundations"></a>
## 1. Design authority foundations — complete

Shared link, stream-row, reach, status, feedback, navigation-ledger, and Merge
Debug foundations are governed and verified.

<a id="state-truth-feedback-recovery"></a>
## 2. State truth, feedback, and recovery — complete

Challenge lifecycle, discussion capability, Gallery media states, Spend
Limits, and Activation Codes have truthful loading, error, permission, and
recovery contracts.

<a id="flagship-redesigns"></a>
## 3. Flagship redesigns — complete

Challenges, Development, Settings and Members, Create App, and the remaining
stream grammar landed with independent craft acceptance.

<a id="semantic-responsive-contracts"></a>
## 4. Semantic and responsive contracts — complete

Narrow-header identity and all six measured coarse-pointer reach classes are
closed.

<a id="bounded-migrations"></a>
## 5. Bounded migrations — complete

Account, route navigation, icon geometry, search controls, bounded-selection
fields, and mobile microcopy are fully governed.

<a id="evidence-live-validation"></a>
## 6. Evidence repair and live validation — complete

The background-fill recapture ledger reached governed zero, the owner live pass
closed, and nested routes now have a single exit.

<a id="app-stage-recomposition"></a>
## 6b. App-stage recomposition — complete

Hosted applications use the ordinary platform shell and a distinct rounded app
card. The corrected boundary was accepted at `2fbe8c5`.

<a id="final-interface-polish"></a>
## Final interface polish — complete

Responsive Challenge rows, Development reading order, bounded-selection reach
and narration, the dead TopBar placement mode, and governed imagery and motion
dispositions were accepted at `266fb7d`.

<a id="audit-skill-extraction"></a>
## 7. Audit-skill extraction — in progress

The owner selected another route group in this application as the unseen
promotion corpus. Lead-claude owns method and calibration specification draft
2; lead-codex owns scripts after that boundary is accepted; promotion follows
on the proven-unseen route group.

<a id="tooling-commit-grammar"></a>
## Tooling: commit grammar — complete

The repository contains the blessed commit grammar, this stable-anchor plan,
and a commit-message hook enforcing a 72-character subject, a final parseable
trailer block, and a non-empty `Task:` trailer for new commits. Existing history
is intentionally unchanged.

<a id="deferred-after-cutover"></a>
## Deferred after cutover

- Do not push local `main` commits until the owner authorizes it.
- Keep the ignored `.env.backup` until the cutover archive step.
- Treat radical Development simplification as a future conversation-first
  redesign, not interface-polish residue.
