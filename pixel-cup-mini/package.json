{
  "name": "pixel-cup-mini",
  "version": "1.0.0",
  "private": true,
  "description": "Pixel Cup Mini — a 2D pixel-art arcade futsal game (Phaser 3 + TypeScript)",
  "main": "server.js",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch & node --watch server.js",
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "phaser": "^3.80.1",
    "typescript": "^5.5.4"
  }
}
