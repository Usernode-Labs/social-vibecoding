import { expect, test } from "@playwright/test"
import AxeBuilder from "@axe-core/playwright"
const session = { session: { id: 41, app_slug: "recipebot", app_name: "RecipeBot", branch_name: "dev/pantry", pr_title: null, session_title: "Pantry filters", status: "active", created_at: "2026-07-28T12:00:00Z" }, messages: [{ id: 3, role: "system", content: "Claude Code finished", model: null, token_count: null, cost_cents: null, created_at: "2026-07-28T12:00:00Z", metadata: { progressLog: ["Fetching main…", "Merging origin/main…"], ccOutput: "Implemented pantry filters.", ccLog: "warning: cache missed" } }] }
test.beforeEach(async ({ page }) => { await page.route("**/api/sessions/41/status", (r) => r.fulfill({ json: { busy: false, progress: [], estimate: null, resolving: false, sync: null } })); await page.route("**/api/sessions/41", (r) => r.fulfill({ json: session })) })
test("renders collapsed persisted logs without interpreting raw metadata as HTML", async ({ page }) => { await page.goto("/react/apps/recipebot/dev/sessions/41"); const timeline = page.getByTestId("dev-build-timeline"); await expect(timeline).toContainText("Build timeline"); await expect(page.getByText("Fetching main…")).not.toBeVisible(); await timeline.locator("summary").nth(1).click(); await expect(page.getByText("Implemented pantry filters.")).toBeVisible() })
test("has no critical or serious mobile accessibility violations", async ({ page }) => { await page.setViewportSize({ width: 390, height: 844 }); await page.goto("/react/apps/recipebot/dev/sessions/41"); const result = await new AxeBuilder({ page }).analyze(); expect(result.violations.filter(({ impact }) => impact === "critical" || impact === "serious")).toEqual([]) })
