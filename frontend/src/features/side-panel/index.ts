/**
 * The side panel beside a running app, on a desktop-width window. See
 * ./controller.ts for the whole of it. ./mount.ts installs the seams and is
 * imported from main.tsx, at module scope, like ../nav/mount.ts.
 */

export { SidePanel } from './side-panel';
