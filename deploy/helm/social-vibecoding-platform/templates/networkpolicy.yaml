{{- if and .Values.enabled .Values.networkPolicy.enabled (or .Values.platform.enabled .Values.migration.enabled .Values.postgresql.enabled) }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "social-vibecoding-platform.fullname" . }}-default-deny
  labels:
    {{- include "social-vibecoding-platform.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: "-3"
spec:
  # Never select the whole namespace: CNPG and the source tunnel are managed
  # by a separate Argo application with their own policy boundary.
  podSelector:
    matchLabels:
      {{- include "social-vibecoding-platform.selectorLabels" . | nindent 6 }}
  policyTypes: [Ingress, Egress]
{{- if .Values.platform.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "social-vibecoding-platform.fullname" . }}-platform
  labels:
    {{- include "social-vibecoding-platform.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: "-3"
spec:
  podSelector:
    matchLabels:
      {{- include "social-vibecoding-platform.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: platform
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        {{- range .Values.networkPolicy.internalCallerNamespaces }}
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ . | quote }}
        {{- end }}
      ports: [{protocol: TCP, port: 3000}]
  egress:
    - to:
        - {{- if and (not .Values.postgresql.enabled) .Values.postgresql.namespace }}
          namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.postgresql.namespace | quote }}
          podSelector:
            matchLabels:
              {{- include "social-vibecoding-platform.postgresqlPodSelector" . | nindent 14 }}
          {{- else }}
          podSelector:
            matchLabels:
              {{- include "social-vibecoding-platform.postgresqlPodSelector" . | nindent 14 }}
          {{- end }}
      ports: [{protocol: TCP, port: {{ .Values.postgresql.port }}}]
    {{- if .Values.networkPolicy.nodeRpc.enabled }}
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ required "networkPolicy.nodeRpc.namespace is required when nodeRpc access is enabled" .Values.networkPolicy.nodeRpc.namespace | quote }}
          {{- with .Values.networkPolicy.nodeRpc.podSelector }}
          podSelector:
            matchLabels:
              {{- toYaml . | nindent 14 }}
          {{- end }}
      ports:
        - protocol: TCP
          port: {{ .Values.networkPolicy.nodeRpc.port }}
    {{- end }}
    {{- if .Values.networkPolicy.explorer.enabled }}
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ required "networkPolicy.explorer.namespace is required when explorer access is enabled" .Values.networkPolicy.explorer.namespace | quote }}
          {{- with .Values.networkPolicy.explorer.podSelector }}
          podSelector:
            matchLabels:
              {{- toYaml . | nindent 14 }}
          {{- end }}
      ports:
        - protocol: TCP
          port: {{ .Values.networkPolicy.explorer.port }}
    {{- end }}
    - ports: [{protocol: UDP, port: 53}, {protocol: TCP, port: 53}]
    - to: [{ipBlock: {cidr: 0.0.0.0/0}}]
      ports: [{protocol: TCP, port: 443}]
{{- end }}
{{- if .Values.migration.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "social-vibecoding-platform.fullname" . }}-migration
  labels:
    {{- include "social-vibecoding-platform.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: "-3"
spec:
  podSelector:
    matchLabels:
      {{- include "social-vibecoding-platform.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: migration
  policyTypes: [Egress]
  egress:
    - to:
        - {{- if and (not .Values.postgresql.enabled) .Values.postgresql.namespace }}
          namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.postgresql.namespace | quote }}
          podSelector:
            matchLabels:
              {{- include "social-vibecoding-platform.postgresqlPodSelector" . | nindent 14 }}
          {{- else }}
          podSelector:
            matchLabels:
              {{- include "social-vibecoding-platform.postgresqlPodSelector" . | nindent 14 }}
          {{- end }}
      ports: [{protocol: TCP, port: {{ .Values.postgresql.port }}}]
    - ports: [{protocol: UDP, port: 53}, {protocol: TCP, port: 53}]
{{- end }}
{{- if .Values.postgresql.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "social-vibecoding-platform.fullname" . }}-postgresql
  labels:
    {{- include "social-vibecoding-platform.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: "-3"
spec:
  podSelector:
    matchLabels:
      {{- include "social-vibecoding-platform.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: postgresql
  policyTypes: [Ingress]
  ingress:
    - from:
        {{- if .Values.platform.enabled }}
        - podSelector:
            matchLabels:
              {{- include "social-vibecoding-platform.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: platform
        {{- end }}
        {{- if .Values.migration.enabled }}
        - podSelector:
            matchLabels:
              {{- include "social-vibecoding-platform.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: migration
        {{- end }}
        {{- range .Values.networkPolicy.databaseCallerNamespaces }}
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ . | quote }}
          podSelector:
            matchLabels:
              app.kubernetes.io/managed-by: social-vibecoding-runtime
              app.kubernetes.io/part-of: social-vibecoding
        {{- end }}
      ports: [{protocol: TCP, port: {{ .Values.postgresql.port }}}]
{{- end }}
{{- if and .Values.platform.enabled .Values.networkPolicy.ciliumApiServerEgress }}
---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: {{ include "social-vibecoding-platform.fullname" . }}-kube-apiserver
  labels:
    {{- include "social-vibecoding-platform.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: "-3"
spec:
  endpointSelector:
    matchLabels:
      {{- include "social-vibecoding-platform.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: platform
  ingress:
    - fromEntities: [ingress]
      toPorts:
        - ports:
            - {port: "3000", protocol: TCP}
  egress:
    - toEntities: [kube-apiserver]
{{- end }}
{{- end }}
