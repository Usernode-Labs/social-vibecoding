{
  "secrets": [
    {
      "key": "GITHUB_APP_ID",
      "required": true,
      "description": "GitHub App ID for the bot account that owns app repos. Without this, app creation fails and no PRs can be opened."
    },
    {
      "key": "GITHUB_PRIVATE_KEY",
      "required": true,
      "sensitive": true,
      "description": "PEM private key for the GitHub App. Single-line with literal \\n escape sequences; the loader un-escapes at startup."
    },
    {
      "key": "GITHUB_BOT_TOKEN",
      "required": true,
      "sensitive": true,
      "description": "Classic PAT with `repo` scope for the bot user. Powers repo creation, branch pushes, PR creation, and the in-app feedback flow (Issues:write on USERNODE_PLATFORM_REPO)."
    },
    {
      "key": "ANTHROPIC_API_KEY",
      "required": false,
      "sensitive": true,
      "description": "Platform-wide fallback Claude key. Users can override with their own via BYOK; if neither is set, Mayor-driven flows are unavailable."
    },
    {
      "key": "ADMIN_USERNAME",
      "required": true,
      "description": "Bootstrap admin username. The first row in `users` is created with this name and is_admin=true on first boot."
    },
    {
      "key": "ADMIN_PASSWORD",
      "required": true,
      "sensitive": true,
      "description": "Bootstrap admin password. Hashed at first boot — rotating only takes effect on a fresh DB or via direct SQL update."
    },
    {
      "key": "JWT_SECRET",
      "required": true,
      "sensitive": true,
      "description": "Signing key for session JWTs and the iframe-issued tokens child apps verify (see app-conventions.md). Required at boot — config.js refuses to start without it. Rotating invalidates every active session."
    },
    {
      "key": "SESSION_SECRET",
      "required": true,
      "sensitive": true,
      "description": "Cookie-signing secret for express-session. Required at boot. Rotating invalidates every active session cookie."
    },
    {
      "key": "USERNODE_DB_PASSWORD",
      "required": true,
      "sensitive": true,
      "description": "Postgres superuser password the platform connects with. Used by docker-compose.yml to seed the postgres container, by db-manager.js to spawn per-app databases, and baked into DATABASE_URL for the platform itself. Rotating means rolling the postgres container too."
    },
    {
      "key": "USERNODE_APP_PUBKEY",
      "required": false,
      "description": "Platform's on-chain wallet public key for the Usernode social-vibecoding linking flow (chain-poller + auth wallet-link in routes/auth.js). When unset, wallet-linking is gracefully disabled — the platform still works without it."
    },
    {
      "key": "USERNODE_APP_SECRET_KEY",
      "required": false,
      "sensitive": true,
      "description": "Private counterpart to USERNODE_APP_PUBKEY. Pre-staged in deploy.yml so any future platform-originated wallet-send flow can use it without a redeploy — currently no runtime consumer reads it. Always set together with USERNODE_APP_PUBKEY."
    }
  ]
}
